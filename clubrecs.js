import React from 'react';
import Read from './crform';

export default function ClubRecs() {
    return(
        <div>
            <h2 className = "cr">Have a Look at the Newest Golf Clubs!</h2>
            <Read />
        </div>
    );
}